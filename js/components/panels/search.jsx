/**
 * Agent Smith Search Panel
 * =========================
 *
 * Panel giving enabling the user to search for specific nodes in the current
 * graph.
 */
var React = require('react'),
    {RouteHandler} = require('react-router');

module.exports = React.createClass({
  render: function() {
    return <h2>Search</h2>;
  }
});
