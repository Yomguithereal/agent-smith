/**
 * Agent Smith Information Panel
 * ==============================
 *
 * Temporary panel displaying basic information about node selection.
 */
var React = require('react'),
    {RouteHandler} = require('react-router');

module.exports = React.createClass({
  render: function() {
    return (
      <div>
        <h2>Node Information</h2>
      </div>
    );
  }
});
